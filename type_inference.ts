// Type Inference: Type inference in TS refers to the ability of the TS compiler to automatically determine & assign types to variables, expressions & function return values based on their usage & context in the code.

const myName = "Walid";
let myAge = 24;
let isMarried = false;
let isStudent = true;

// Practice 01:
let message = "Hello, TypeScript!";
// console.log(message);

// Prctice 02:
const calculateArea = (a: number, b: number) => {
    return a * b;
}

// console.log(calculateArea(5, 3));